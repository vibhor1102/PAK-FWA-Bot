services:
  - type: web
    name: pak-fwa-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m bot
    healthCheckPath: /health
    autoDeploy: true
