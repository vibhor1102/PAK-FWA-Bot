services:
  - type: web
    name: pak-fwa-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m bot
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
    autoDeploy: true
